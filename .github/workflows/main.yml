name: Production CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    name: Build & Test Backend
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Run Maven Tests & Build
      run: ./mvnw clean package
      
    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: spring-boot-app
        path: target/*.jar

  docker-verify:
    name: Verify Docker Container Build
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
    - uses: actions/checkout@v4
    
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Build and Push Docker Image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/study-platform:latest

#  deploy-to-server:
#    name: Deploy to Production Server
#    runs-on: ubuntu-latest
#    needs: docker-verify
#    steps:
#    - name: Executing Remote SSH Commands for Deployment
#      uses: appleboy/ssh-action@v1.0.3
#      with:
#        host: ${{ secrets.SERVER_IP }}
#        username: ${{ secrets.SERVER_USERNAME }}
#        key: ${{ secrets.SERVER_SSH_KEY }}
#        script: |
#          # 1. Stop current containers smoothly
#          docker stop study-platform-app || true
#          docker rm study-platform-app || true
#          
#          # 2. Force pull the absolute latest architecture we just uploaded to DockerHub
#          docker pull ${{ secrets.DOCKER_USERNAME }}/study-platform:latest
#          
#          # 3. Spin up the new infrastructure mapping it to port 80 (HTTP)
#          docker run -d \
#            --name study-platform-app \
#            -p 80:8080 \
#            --env SPRING_DATASOURCE_URL=${{ secrets.PROD_DB_URL }} \
#            --env SPRING_DATASOURCE_USERNAME=${{ secrets.PROD_DB_USER }} \
#            --env SPRING_DATASOURCE_PASSWORD=${{ secrets.PROD_DB_PASS }} \
#            --restart unless-stopped \
#            ${{ secrets.DOCKER_USERNAME }}/study-platform:latest
